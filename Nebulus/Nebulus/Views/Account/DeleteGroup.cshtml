@model Nebulus.Models.SecurityRoleEntity

@{
    ViewBag.Title = "DeleteGroup";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>DeleteGroup</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.IdentityRole)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IdentityRole)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> 
            @Html.ActionLink("Cancel", "ManageUsers", "", new { @class = "btn btn-default" })
        </div>
    }
</div>
